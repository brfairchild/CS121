import java.util.HashSet;
import java.util.Set;

public class SetNotes {
    // Set : one way to group like objects together and handle as one unit.
    // Java Collection Framework > set of classes and interfaces for working with collections of objects.

    // To create a set Set<type>name=?

   // Set<String> movieName = new Set();
    // set is an interface and we cannot create a instance of an interface
    // So we do the below method.
    Set<String> tMovieName = new HashSet<>();

    // how do we add to our set?
    // simply use the .add() method passing in the element we would like to add.

    //tMovieName.add("Shrek");
    //System.out.print(tMovieName);



}
